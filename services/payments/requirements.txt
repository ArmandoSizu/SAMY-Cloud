# =============================================================================
# Microservicio de Pagos - dependencias
# =============================================================================
# Deliberadamente mas corto que el del Core: este servicio no sirve HTML, no
# tiene sesiones, no tiene admin y no compila CSS. Cada dependencia que no
# esta es superficie de ataque que no hay que parchear.
# =============================================================================

Django==5.2.17
djangorestframework==3.18.0
drf-spectacular==0.30.0

psycopg[binary,pool]==3.3.5
django-environ==0.14.0

redis==6.4.0
celery==5.6.3

structlog==26.1.0
httpx==0.28.1
tenacity==9.1.4

# Verificacion de firmas RSA de webhooks (Conekta usa RSA-SHA256).
cryptography==50.0.1

# Generacion del QR de cobro.
qrcode[pil]==8.2

gunicorn==26.2.0
